<h1> Bem vindo ao paynow </h1>

<%= form_with url: search_receipt_path, model: @receipt, params: :receipt_token do |f| %>>
  <%= f.label :receipt_token, 'Código do Recibo' %>
  <%= f.text_field :receipt_token %>
  <%= f.submit 'Visualizar'%>
<% end %>