<% unless @client_company.orders.empty? %>
  <% @client_company.orders.each do |order|%>
    <%= order.order_token %>
    <%= order.client_product.name %>
    <%= number_to_currency order.price %>
    <%= number_to_currency order.price_discounted %>
    <%= order.client_external.name %>
    <%= order.client_external.cpf %>
  <% end %>
<% else %>
  <h1> Nenhum cobrança cadastrada </h1>
<% end %>